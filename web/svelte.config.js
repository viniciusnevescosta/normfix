// Svelte's compiler options for this app.
//
// Runes only: the playground's state is shared between a tree, an editor and a
// result panel, and letting some of it be implicit reactivity and some of it
// explicit is how the imperative version got confusing in the first place.
export default {
  compilerOptions: {
    runes: true,
  },
};
